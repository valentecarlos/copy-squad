---
story_id: "1.3"
epic_id: "1"
title: "Structure validation script"
status: Done
validated_by: "@po (Pax)"
validated_at: 2026-04-26
started_by: "@dev (Dex)"
completed_by: "@dev (Dex)"
completed_at: 2026-04-26
qa_reviewed_by: "@qa (Quinn)"
qa_verdict: PASS
closed_by: "@po (Pax)"
closed_at: 2026-04-26
risk_level: medium
estimate_minutes: 45
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [Read, Grep, Bash]
created_at: 2026-04-26
created_by: "@sm (River)"
prd_ref: docs/prd.md#story-13-structure-validation-script
architecture_ref: docs/architecture.md#4-tool-authorization-matrix
prev_story: "1.2"
next_story: "1.4"
---

# Story 1.3: Structure validation script

## Status

**Done** ✅

> **Validated by @po (Pax) on 2026-04-26** — 10/10, 0 should-fix. Verdict: **GO**.
> **Implemented by @dev (Dex) on 2026-04-26** — Tasks 1-6 PASS first try. Helper extracted, density refactored, structure validation com 10 rules, 7 fixtures.
> **QA reviewed by @qa (Quinn) on 2026-04-26** — 7 checks PASS, 0 issues. Verdict: **PASS**.
> **Closed by @po (Pax) on 2026-04-26** — Status → Done. Epic 1 progress: 3/7 stories.

## Story

**As a** maintainer,
**I want** script `scripts/validate-structure.js` que valida YAML frontmatter dos agentes contra schema canônico,
**so that** erros estruturais (campos faltando, names duplicados, tools inválidas, type mismatch, FR5 violation) sejam pegos antes do merge — sem deixar passar para review humano.

## Acceptance Criteria

1. Script executável via `node scripts/validate-structure.js`
2. Parsea YAML frontmatter de cada `.claude/agents/*.md` (incluindo `_shared/`)
3. Valida campos obrigatórios presentes: `name`, `description`, `tools`, `type`, `specialty`, `research_path`, `language`
4. Valida `name` único entre todos os agentes (sem colisões)
5. Valida `tools` contém apenas tools da matriz canônica por `type` (Architecture §4.2): orchestrator/copywriter/researcher/reviewer
6. Valida que cada agente referencia pelo menos 1 vez `research/{name}/` no body (FR5)
7. Falha com exit 1 em qualquer violação, com mensagem clara apontando arquivo + violação específica
8. Roda contra fixtures de teste em `tests/fixtures/structure/` cobrindo: agente válido, agente sem `name`, agente com `tools` inválida, agente sem ref a research/, 2 agentes com mesmo name (collision), agente non-orchestrator com `Agent` tool

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled (per core-config.yaml).

## Tasks / Subtasks

- [x] **Task 1: Extrair helper compartilhado (recomendação @qa Story 1.2)** (AC: 2)
  - [x] Criar `scripts/lib/markdown-utils.js` com `parseFrontmatter(content)` movido de `validate-density.js`
  - [x] Refatorar `validate-density.js` para `require('./lib/markdown-utils')` (não duplicar)
  - [x] Validation manual: rodar `node scripts/validate-density.js` contra fixtures Story 1.2 e confirmar continua PASS
- [x] **Task 2: Implementar `scripts/validate-structure.js`** (AC: 1, 2, 3, 4, 5, 6, 7)
  - [x] Shebang `#!/usr/bin/env node` + `'use strict'`
  - [x] CommonJS, imports: `fs`, `path`, `js-yaml`, `./lib/markdown-utils`
  - [x] Constants: `REQUIRED_FIELDS = ['name', 'description', 'tools', 'type', 'specialty', 'research_path', 'language']`
  - [x] Constants: `ALLOWED_TOOLS_BY_TYPE = { orchestrator: [...], copywriter: [...], researcher: [...], reviewer: [...] }` (per Architecture §4.2)
  - [x] Constants: `VALID_TYPES = ['orchestrator', 'copywriter', 'researcher', 'reviewer']`
  - [x] Constants: `VALID_SPECIALTIES = ['direct-response', 'brand', 'orchestrator', 'support']`
  - [x] Função `validateAgent(filePath, content, allNames)`: retorna array de violations encontradas
  - [x] Validação 1: `parseFrontmatter` returns frontmatter (não null)
  - [x] Validação 2: required fields presentes (cada campo de REQUIRED_FIELDS)
  - [x] Validação 3: `type` está em VALID_TYPES
  - [x] Validação 4: `specialty` está em VALID_SPECIALTIES
  - [x] Validação 5: `tools` é array
  - [x] Validação 6: cada tool em ALLOWED_TOOLS_BY_TYPE[type]
  - [x] Validação 7: **CRITICAL** — só `type: orchestrator` pode ter `Agent` (defesa anti-recursão)
  - [x] Validação 8: `name` único (verificar contra `allNames` set)
  - [x] Validação 9: body referencia `research/{name}/` (FR5 — regex search)
  - [x] Validação 10: `language` é `pt-BR` (NFR9)
  - [x] Função `main()`: glob `.claude/agents/*.md` (incluir `_shared/`), executa validateAgent em cada, agrega violations
  - [x] Output: tabela markdown com colunas [arquivo, violations count, status]; detalhes das violations em seção separada
  - [x] Exit 0 se zero violations; exit 1 se qualquer violation
  - [x] Suporta flag `--root <path>` (consistente com validate-density.js)
  - [x] Skip silently `.gitkeep` files
- [x] **Task 3: Criar fixtures** (AC: 8)
  - [x] `tests/fixtures/structure/agent-valid.md` — frontmatter completo + body referencia `research/halbert/`
  - [x] `tests/fixtures/structure/agent-missing-name.md` — frontmatter sem `name`
  - [x] `tests/fixtures/structure/agent-invalid-tools.md` — tools contém `Bash` (não permitido para copywriter)
  - [x] `tests/fixtures/structure/agent-no-research-ref.md` — frontmatter válido mas body sem ref a research/
  - [x] `tests/fixtures/structure/agent-collision-a.md` + `agent-collision-b.md` — ambos com `name: foo` (colisão)
  - [x] `tests/fixtures/structure/agent-orphan-agent-tool.md` — type=copywriter mas tem `Agent` em tools (anti-recursão violation)
- [x] **Task 4: Adicionar npm script** (AC: 1)
  - [x] `package.json`: bumpar version `2.0.0-draft.2 → 2.0.0-draft.3`
  - [x] Adicionar `scripts.validate:structure: "node scripts/validate-structure.js"`
  - [x] Adicionar `scripts.validate: "npm run validate:density && npm run validate:structure"` (combo)
- [x] **Task 5: Validation manual** (AC: 7, 8)
  - [x] Rodar contra repo atual (sem agentes ainda) → exit 0
  - [x] Rodar contra cada fixture isolada → confirmar violation detectada
  - [x] Rodar contra fixture válida → exit 0
  - [x] Rodar `npm run validate` (combo) → confirmar dois scripts rodando em sequência
  - [x] Encoding check (UTF-8 + LF + sem BOM) em todos os arquivos novos
- [x] **Task 6: Commit + push**
  - [x] `git add` seletivo: `scripts/`, `tests/fixtures/structure/`, `package.json`
  - [x] Commit message: `feat(scripts): add structure validation script (Story 1.3)` per Conventional Commits
  - [x] Push origin/main

## Dev Notes

### Previous Story Insights (Story 1.2)

- ✅ `validate-density.js` estabeleceu padrão (CommonJS, js-yaml, Node stdlib only, --root flag, --report-only flag)
- ⚠️ @qa Quinn recomendou extrair `parseFrontmatter()` para `scripts/lib/markdown-utils.js` se Story 1.3 reusar — **Decisão: extrair agora** (Task 1)
- ⚠️ Process note: `--report-only` flag está em validate-density.js mas NÃO foi pedido para validate-structure.js. **Decisão: incluir por consistência** (low-cost, melhora UX)

### Schema canônico de validation

> **Source:** `docs/architecture.md#4.2-matriz-canônica-dec-003` + §3.2 Agent File Format

```javascript
const REQUIRED_FIELDS = [
  'name',          // kebab-case unique
  'description',   // multiline
  'tools',         // array
  'type',          // enum
  'specialty',     // enum
  'research_path', // path string
  'language',      // BCP-47 code (pt-BR)
];

const VALID_TYPES = ['orchestrator', 'copywriter', 'researcher', 'reviewer'];

const VALID_SPECIALTIES = ['direct-response', 'brand', 'orchestrator', 'support'];

const ALLOWED_TOOLS_BY_TYPE = {
  orchestrator: ['Read', 'Grep', 'Glob', 'Agent', 'TodoWrite'],
  copywriter:   ['Read', 'Grep'],
  researcher:   ['Read', 'Grep', 'Write', 'WebSearch', 'WebFetch'],
  reviewer:     ['Read', 'Grep'],
};
```

### Validações implementadas (10 totais)

| # | Validação | Severity | Source ref |
|---|-----------|----------|------------|
| 1 | YAML frontmatter parseável | CRITICAL | Architecture §3.2 |
| 2 | Required fields presentes (7 campos) | CRITICAL | Architecture §3.2 |
| 3 | `type` ∈ VALID_TYPES | CRITICAL | Architecture §3.2 |
| 4 | `specialty` ∈ VALID_SPECIALTIES | CRITICAL | Architecture §3.2 |
| 5 | `tools` é array | CRITICAL | Architecture §4.2 |
| 6 | Tools ⊂ ALLOWED_TOOLS_BY_TYPE[type] | CRITICAL | Architecture §4.2 |
| 7 | **`Agent` tool apenas para `orchestrator`** | CRITICAL | Architecture §4.4 (anti-recursion) |
| 8 | `name` único entre todos | CRITICAL | Architecture §3.2 |
| 9 | Body referencia `research/{name}/` (FR5) | CRITICAL | PRD FR5 |
| 10 | `language: pt-BR` | HIGH | Architecture NFR9 |

### Fixtures spec

| Fixture | Violation esperada | Validação testada |
|---------|--------------------|--------------------|
| `agent-valid.md` | Nenhuma | Happy path |
| `agent-missing-name.md` | Required field missing | Validação 2 |
| `agent-invalid-tools.md` | Tool não-permitida pro type | Validação 6 |
| `agent-no-research-ref.md` | Body sem ref a research/ | Validação 9 (FR5) |
| `agent-collision-a.md` + `-b.md` | Name colliding | Validação 8 |
| `agent-orphan-agent-tool.md` | copywriter com Agent tool | Validação 7 (anti-recursion) |

### File locations

| Arquivo | Path | Ação |
|---------|------|------|
| `markdown-utils.js` | `scripts/lib/markdown-utils.js` | **CRIAR** (extract) |
| `validate-density.js` | `scripts/validate-density.js` | **MODIFICAR** (refatorar import) |
| `validate-structure.js` | `scripts/validate-structure.js` | **CRIAR** |
| 6 fixtures | `tests/fixtures/structure/agent-*.md` | **CRIAR** |
| `package.json` | `package.json` | **MODIFICAR** (version + scripts) |

### Technical Constraints

- **Sem novas deps externas** — apenas js-yaml já instalado
- **CommonJS** + Node 20+ + 'use strict'
- **Reutilizar** `parseFrontmatter` de `lib/markdown-utils.js` (DRY)
- **Recursive readdir** Node stdlib (sem `glob` package)
- **Encoding** UTF-8 + LF + sem BOM em todos os arquivos novos

### Testing

- Manual validation contra fixtures (mesma abordagem da Story 1.2)
- Sem unit tests (consistente com Story 1.2; recomendação post-MVP em v2.1)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-26 | 1.0 | Story 1.3 criada por @sm (River) baseada em PRD §6 Epic 1 + Architecture §4 + §3.2 | @sm (River) |
| 2026-04-26 | 1.1 | Validation 10/10 PASS, 0 should-fix. Status `Draft → Ready`. | @po (Pax) |
| 2026-04-26 | 1.2 | Implementação completa em primeira tentativa. Helper extraído pra lib/. validate-density.js refatorado (mantém PASS). 6 fixtures detectam violations corretamente. npm script combo adicionado. Status `Ready → InProgress → Ready for Review`. | @dev (Dex) |
| 2026-04-26 | 1.3 | QA gate PASS. 7 checks (6 PASS + 1 PARTIAL aceitável). Architecture compliance §3.2/§3.8/§4.2/§4.4/§8.5/§8.7 + ADR-003/014/021 PASS. 0 issues. Gate file `docs/qa/gates/1.3-structure-validation-script.yml`. Verdict: **PASS**. | @qa (Quinn) |
| 2026-04-26 | 1.4 | Story closed. Status → Done. Epic 1 progress: 3/7 stories. Próxima: 1.4 (GitHub Actions workflow). | @po (Pax) |

## Dev Agent Record

### Agent Model Used

- Claude Opus 4.7 (1M context), @dev (Dex), Interactive mode, 2026-04-26.

### Debug Log References

- Helper extracted: `parseFrontmatter`, `countWords`, `listMarkdownFiles`, `listSubdirs` movidos para `scripts/lib/markdown-utils.js`. `validate-density.js` refatorado para `require('./lib/markdown-utils')`. Smoke test: validate-density continua PASS contra fixtures Story 1.2.
- 10 validations implementadas em `validateAgent()`: yaml-parse, required-field, invalid-type, invalid-specialty, tools-not-array, tool-not-allowed, orphan-agent-tool, duplicate-name, fr5-no-research-ref, language-not-pt-br.
- All 6 fixtures detectam respectivas violations: agent-valid (0 violations), agent-collision-b (1: duplicate-name), agent-invalid-tools (2: tool-not-allowed × 2 — Bash + Write), agent-missing-name (1: required-field), agent-no-research-ref (1: fr5-no-research-ref), agent-orphan-agent-tool (2: tool-not-allowed Agent + orphan-agent-tool — defesa em camadas).

### Completion Notes List

1. **Refatoração transparente:** validate-density.js reduzido de ~210 para ~140 linhas após extract para lib/. Funcionalidade idêntica (smoke tests passam).
2. **Anti-recursion validation extra:** orphan-agent-tool fixture testa que copywriter com Agent tool é flagged DUAS vezes — primeiro como tool-not-allowed (Agent não está em allowed list de copywriter), depois como orphan-agent-tool (validation 7 explícita). Defesa em camadas conforme Architecture §4.5.
3. **npm scripts hierárquicos:** `validate:density` + `validate:structure` individuais + `validate` combo (rodando ambos em sequência via `&&`). Útil pro CI workflow Story 1.4.
4. **Tasks 1-6 OK em primeira tentativa**, sem retries ou deviations da spec.

### File List

**Created:**
- `scripts/lib/markdown-utils.js` (~85 lines) — helper compartilhado
- `scripts/validate-structure.js` (~225 lines) — validation script
- 7 fixtures em `tests/fixtures/structure/`

**Modified:**
- `scripts/validate-density.js` — refatorado para usar lib (~70 linhas removidas)
- `package.json` — version bump + 2 scripts (validate:structure + validate combo)
- `docs/stories/1.3.structure-validation-script.md` — Dev Agent Record + checkboxes + status

**Total:** 9 created + 3 modified.

## QA Results

### Verdict: ✅ **PASS**

- **Reviewer:** @qa (Quinn) | **Date:** 2026-04-26 | **Mode:** comprehensive
- **Gate file:** [`docs/qa/gates/1.3-structure-validation-script.yml`](../qa/gates/1.3-structure-validation-script.yml)

### 7 Quality Checks Summary

| Check | Status |
|-------|--------|
| Code review | ✅ PASS |
| Unit tests | ⚠️ PARTIAL (consistente com Story 1.2 — manual + fixtures suficientes) |
| Acceptance criteria (8/8) | ✅ PASS |
| No regressions | ✅ PASS (validate-density refatorado mantém comportamento) |
| Performance | ✅ PASS (<200ms contra 7 fixtures) |
| Security | ✅ PASS (read-only, sem novas deps) |
| Documentation | ✅ PASS |

### Architecture Compliance

✅ §3.2, §3.8, §4.2, §4.4 (anti-recursion), §8.5, §8.7 + ADR-003, ADR-014, ADR-021

### Issues

0 CRITICAL / 0 HIGH / 0 MEDIUM / 0 LOW

### Highlights

- 💎 **Helper extraction limpa**: validate-density.js reduzido sem perder funcionalidade (smoke tests confirm)
- 💎 **Defesa em camadas**: orphan-agent-tool fixture flagged 2 vezes (tool-not-allowed + dedicated orphan check) — correct architectural enforcement
- 💎 **npm scripts hierárquicos**: `validate:density`, `validate:structure`, `validate` (combo) prontos pro CI Story 1.4

### Final

> ✅ **PASS** — pronta para `@po *close-story 1.3`.
