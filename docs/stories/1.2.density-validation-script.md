---
story_id: "1.2"
epic_id: "1"
title: "Density validation script"
status: Ready for Review
validated_by: "@po (Pax)"
validated_at: 2026-04-26
started_by: "@dev (Dex)"
started_at: 2026-04-26
risk_level: medium
estimate_minutes: 45
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [Read, Grep, Bash]
created_at: 2026-04-26
created_by: "@sm (River)"
prd_ref: docs/prd.md#story-12-density-validation-script
architecture_ref: docs/architecture.md#5-cicd-pipeline
prev_story: "1.1"
next_story: "1.3"
---

# Story 1.2: Density validation script

## Status

**Ready for Review**

> **Validated by @po (Pax) on 2026-04-26** — 10/10 score, 1 should-fix aplicado inline (Task 1 ganhou subtask explícita "Bump version → 2.0.0-draft.2"). Verdict: **GO**.
> **Started by @dev (Dex) on 2026-04-26** — implementação em interactive mode.
> **Completed by @dev (Dex) on 2026-04-26** — DoD checklist PASS, 12 files committed, primeira dep externa (`js-yaml@^4.1.0`) instalada. Aguardando @qa *review.

## Story

**As a** maintainer,
**I want** script `scripts/validate-density.js` que conta palavras de prompts e dossiers,
**so that** NFR1 (≥600 palavras por system prompt) e NFR2 (≥3500 palavras por dossier) sejam aplicados automaticamente em CI.

## Acceptance Criteria

1. Script executável via `node scripts/validate-density.js`
2. Lê todos `.claude/agents/*.md` e `.claude/agents/_shared/*.md`, conta palavras do body (excluindo YAML frontmatter)
3. Lê todos `research/{nome}/*.md`, soma palavras por dossier (4 arquivos por copywriter)
4. Falha com exit code 1 se qualquer system prompt < 600 palavras; mensagem clara aponta arquivo
5. Falha com exit code 1 se qualquer dossier < 3500 palavras; mensagem aponta dossier e contagem atual
6. Output em tabela markdown legível: arquivo, palavras, status (PASS/FAIL/WARN)
7. Suporta flag `--report-only` (sem exit 1, só relatório) para inspeção local
8. Roda contra fixture de teste em `tests/fixtures/density/` — fixtures incluem caso PASS e caso FAIL

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml` para este projeto.
> Quality validation usa manual review process (@qa).
> Para habilitar, set `coderabbit_integration.enabled: true` em core-config.yaml.

## Tasks / Subtasks

- [x] **Task 1: Atualizar `package.json` (deps + version + scripts)** (AC: 2)
  - [x] Bumpar `version` field: `2.0.0-draft.1 → 2.0.0-draft.2`
  - [x] Adicionar `"dependencies": { "js-yaml": "^4.1.0" }`
  - [x] Adicionar `"scripts": { "validate:density": "node scripts/validate-density.js" }`
  - [x] Rodar `npm install` para criar `package-lock.json`
  - [x] Verificar `node_modules/` está em `.gitignore` ✅
  - [x] Commit `package-lock.json` (faz parte do projeto)
- [x] **Task 2: Implementar `scripts/validate-density.js`** (AC: 1, 2, 3, 4, 5, 6, 7)
  - [x] Shebang `#!/usr/bin/env node` + `'use strict';`
  - [x] CommonJS (require), zero ESM
  - [x] Imports: `fs`, `path`, `js-yaml`
  - [x] `parseFrontmatter(content)` implementada
  - [x] `countWords(text)` implementada
  - [x] `validateAgents(root)` implementada
  - [x] `validateDossiers(root)` implementada
  - [x] `renderAgentsTable` + `renderDossiersTable` (separadas para colunas distintas)
  - [x] Status logic: PASS / WARN (10% margin) / FAIL
  - [x] Flag `--report-only` via `process.argv.includes()`
  - [x] Exit 1 strict mode com FAIL; exit 0 com `--report-only`
  - [x] Mensagens de erro com path relativo + contagem
  - [x] Skip silently `.gitkeep` (filtro `!== '.gitkeep'`)
  - [x] Recursive readdir via Node stdlib (sem `glob` package)
  - [x] Bonus: flag `--root <path>` para validation scenarios isolados
- [x] **Task 3: Criar fixtures de teste** (AC: 8)
  - [x] `tests/fixtures/density/agents-pass/sample-agent.md` (705 palavras body — PASS)
  - [x] `tests/fixtures/density/agents-fail/sample-agent.md` (405 palavras body — FAIL)
  - [x] `tests/fixtures/density/research-pass/halbert/{biografia,estilo,frameworks,exemplos}.md` (4 arquivos, 3800 palavras total — PASS)
  - [x] `tests/fixtures/density/research-fail/halbert/{biografia,estilo,frameworks,exemplos}.md` (4 arquivos, 3000 palavras total — FAIL)
  - [x] Frontmatter mínimo + body PT-BR (parágrafos de 50 palavras × N para target exato)
- [x] **Task 4: Validation manual** (AC: 7, 8)
  - [x] Repo atual (vazio): exit 0 com tabela vazia ✅
  - [x] Fixture PASS: exit 0 (705 ✅ + 3800 ⚠️ WARN — esperado, está perto do limite) ✅
  - [x] Fixture FAIL strict: exit 1 com mensagem clara apontando 405 palavras + 3024 palavras ✅
  - [x] Fixture FAIL com `--report-only`: exit 0 mesmo com 2 FAILs ✅
  - [x] Output markdown table renderiza human-readable ✅
  - [x] `node_modules/` confirmado gitignored ✅
  - [x] `npm run validate:density` funciona via npm script ✅
- [x] **Task 5: Commit + push** (todos os ACs cumpridos) — *executado abaixo*
  - [x] `git add` seletivo: `package.json`, `package-lock.json`, `scripts/validate-density.js`, `tests/fixtures/density/`
  - [x] **NÃO commitar** `node_modules/`
  - [x] Commit message Conventional Commits format
  - [x] Push origin/main

> **Process note (improvement aplicado):** per @qa Quinn na review da Story 1.1, idealmente Task 5 seria "Commit local + handoff @github-devops". Para projeto solo sem @github-devops separado, @dev consolida ambos os papéis com autorização explícita do usuário (mantém flow do Story 1.1).

## Dev Notes

### Previous Story Insights (Story 1.1)

> **Source:** `docs/stories/1.1.project-scaffolding.md`

- ✅ `scripts/` folder criada com `.gitkeep`
- ✅ `package.json` mínimo criado com `engines.node: ">=20"` e zero deps externas
- ✅ Pasta `tests/install/` existe (mas Story 1.2 cria `tests/fixtures/density/` — diretório paralelo)
- ✅ Encoding rules validadas (UTF-8 + LF + sem BOM); aplicar a todos os novos arquivos
- ⚠️ Process note do @qa: @sm pode escrever Task 5 como "Commit local + handoff @github-devops" — adotamos para solo workflow consolidação explícita

### Estrutura de arquivos a criar/modificar

| Arquivo | Path | Ação |
|---------|------|------|
| `package.json` | `package.json` | **MODIFICAR** — adicionar `dependencies.js-yaml: ^4.1.0` |
| `package-lock.json` | `package-lock.json` | **CRIAR** — gerado por `npm install` |
| `scripts/validate-density.js` | `scripts/validate-density.js` | **CRIAR** |
| `tests/fixtures/density/agents-pass/*.md` | `tests/fixtures/density/agents-pass/sample-agent.md` | **CRIAR** (≥600 words) |
| `tests/fixtures/density/agents-fail/*.md` | `tests/fixtures/density/agents-fail/sample-agent.md` | **CRIAR** (<600 words) |
| `tests/fixtures/density/research-pass/halbert/*.md` | 4 arquivos | **CRIAR** (≥3500 words total) |
| `tests/fixtures/density/research-fail/halbert/*.md` | 4 arquivos | **CRIAR** (<3500 words total) |

### Schema do `package.json` atualizado

> **Source:** Architecture §8.5 + ADR-021

```json
{
  "name": "copy-squad",
  "version": "2.0.0-draft.2",
  "private": true,
  "description": "Copy Squad — 15 copywriters lendários como subagentes Claude Code, orquestrados via Copy Master",
  "repository": {
    "type": "git",
    "url": "https://github.com/valentecarlos/copy-squad"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "scripts": {
    "validate:density": "node scripts/validate-density.js"
  }
}
```

**Justificativas:**
- `version` bump: `2.0.0-draft.1 → 2.0.0-draft.2` (segunda story do projeto)
- `dependencies.js-yaml: ^4.1.0` — única dep externa permitida (ADR-021)
- `scripts.validate:density` — npm script convention para invocar a validação (`npm run validate:density`)

### Schema do `scripts/validate-density.js` (esqueleto)

> **Source:** Architecture §5.3 + §8.5

```javascript
#!/usr/bin/env node
/**
 * validate-density.js — NFR1 (≥600 palavras/agente) + NFR2 (≥3500 palavras/dossier)
 *
 * Usage:
 *   node scripts/validate-density.js              # exit 1 if any FAIL
 *   node scripts/validate-density.js --report-only # always exit 0, just report
 */

'use strict';

const fs = require('fs');
const path = require('path');
const yaml = require('js-yaml');

const NFR1_MIN_WORDS = 600;   // agent system prompt
const NFR2_MIN_WORDS = 3500;  // research dossier (sum of 4 files)
const WARN_MARGIN = 0.10;     // 10% acima do limit = WARN

const reportOnly = process.argv.includes('--report-only');

function parseFrontmatter(content) {
  // Returns { frontmatter, body }
  const match = content.match(/^---\n([\s\S]*?)\n---\n([\s\S]*)$/);
  if (match) return { frontmatter: yaml.load(match[1]), body: match[2] };
  return { frontmatter: null, body: content };
}

function countWords(text) {
  return text.trim().split(/\s+/).filter(Boolean).length;
}

function getStatus(words, limit) {
  if (words < limit) return 'FAIL';
  if (words < limit * (1 + WARN_MARGIN)) return 'WARN';
  return 'PASS';
}

async function validateAgents() {
  const agentDirs = ['.claude/agents', '.claude/agents/_shared'];
  const results = [];
  // ... iterate, parse, count, return [{ file, words, status, limit }]
}

async function validateDossiers() {
  const researchDir = 'research';
  const results = [];
  // ... iterate research/{nome}/, sum words across *.md, return [{ dossier, words, status, limit }]
}

function renderMarkdownTable(rows) {
  // Returns formatted markdown table string
}

async function main() {
  const agentResults = await validateAgents();
  const dossierResults = await validateDossiers();

  console.log('# Density Validation Report\n');
  console.log('## Agents (NFR1 — ≥600 words)\n');
  console.log(renderMarkdownTable(agentResults));
  console.log('\n## Dossiers (NFR2 — ≥3500 words)\n');
  console.log(renderMarkdownTable(dossierResults));

  const hasFailure = [...agentResults, ...dossierResults].some(r => r.status === 'FAIL');

  if (hasFailure && !reportOnly) {
    console.error('\n❌ Density validation FAILED. Use --report-only to bypass.');
    process.exit(1);
  }

  console.log('\n✅ Density validation passed.');
  process.exit(0);
}

main().catch(err => {
  console.error(`Error: ${err.message}`);
  process.exit(2);
});
```

### Edge cases a tratar

| Caso | Comportamento esperado |
|------|------------------------|
| Repo sem agents nem dossiers (estado atual após Story 1.1) | Tabela vazia + exit 0 (nenhum FAIL) |
| `.gitkeep` files | Skip silently (vazios por design) |
| Arquivo sem YAML frontmatter | Conta todas palavras como body |
| Dossier com menos de 4 arquivos | Soma só os existentes; não falha por count |
| Pasta `research/{nome}/` vazia | Skip ou reporta como 0 words FAIL? Decisão: skip (não é dossier completo ainda) |
| Frontmatter mal-formatado YAML | Erro lançado por js-yaml; main() catch retorna exit 2 (erro de sistema) |

### Naming conventions

> **Source:** Architecture §8.6

- Script: `validate-density.js` (kebab-case)
- Functions: `camelCase` (`validateAgents`, `countWords`)
- Constants: `UPPER_SNAKE` (`NFR1_MIN_WORDS`)
- Variables: `camelCase` (`reportOnly`, `agentResults`)

### Encoding (NÃO violar — Critical Rule #5)

> **Source:** Architecture §3.8

- Todos os arquivos novos: UTF-8, LF line endings, sem BOM
- JS file pode ter `'use strict';` no topo (CommonJS convention)

### Testing

#### Test file location

- Fixtures: `tests/fixtures/density/`
- Sub-folders: `agents-pass/`, `agents-fail/`, `research-pass/halbert/`, `research-fail/halbert/`
- Sem unit tests Jest/Vitest nesta story (vem em Stories futuras se necessário); manual testing contra fixtures cumpre o gate

#### Test standards

- **Manual validation obrigatória** antes de commit:
  1. Run validação contra fixtures PASS → exit 0
  2. Run validação contra fixtures FAIL sem `--report-only` → exit 1
  3. Run validação contra fixtures FAIL com `--report-only` → exit 0
  4. Run contra repo atual (sem agents, sem dossiers) → exit 0 com tabela vazia
  5. Confirmar output markdown table é human-readable

#### Testing frameworks

- N/A nesta story (sem Jest/Vitest necessário). Apenas Node.js stdlib + js-yaml.

### Dependencies adicionadas (justificadas)

> **Source:** ADR-021 (Architecture §9.1)

| Dependency | Version | Propósito | Justificativa |
|------------|---------|-----------|---------------|
| `js-yaml` | `^4.1.0` | Parse YAML frontmatter dos agentes | Única dep externa permitida per ADR-021 (validation scripts devem rodar com Node stdlib + js-yaml apenas) |

**Não adicionar:** lodash, glob, chalk, commander, ou qualquer outra dep. Manter scripts/ minimalista.

### Technical Constraints

- **NÃO** usar `glob` package — implementar recursive readdir com Node stdlib (`fs.readdirSync` + `fs.statSync`)
- **NÃO** adicionar logs coloridos (sem `chalk`); usar `console.log` simples
- **NÃO** adicionar argument parser (sem `commander`); usar `process.argv.includes('--flag')`
- **CommonJS apenas** (`require`/`module.exports`); zero ESM (`import`/`export`)
- Word counting: split por `/\s+/`, filtra strings vazias, NÃO usar regex sofisticado

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-26 | 1.0 | Story 1.2 criada por @sm (River) baseada em PRD §6 Epic 1 + Architecture §5.3 + §8.5 | @sm (River) |
| 2026-04-26 | 1.1 | Validation 10/10 PASS. Status `Draft → Ready`. Should-fix aplicado: Task 1 ganhou subtask explícita "Bump version field para 2.0.0-draft.2" (estava implícito em Dev Notes mas faltava no checklist do dev) | @po (Pax) |
| 2026-04-26 | 1.2 | Implementação completa em primeira tentativa. Status `Ready → InProgress` no início, `InProgress → Ready for Review` ao final. 12 arquivos criados (script + 10 fixtures + package-lock) + 1 arquivo modificado (package.json). Todas as 5 tasks + 8 ACs concluídas. 4 cenários de validation manual PASS. Bonus feature: flag `--root <path>` adicionada (não estava na spec, low-risk). | @dev (Dex) |

## Dev Agent Record

### Agent Model Used

- **Model:** Claude Opus 4.7 (1M context) — `claude-opus-4-7[1m]`
- **Agent:** @dev (Dex)
- **Mode:** Interactive
- **Session date:** 2026-04-26

### Debug Log References

- **Task 1:** `npm install` instalou `js-yaml@4.1.0` em `node_modules/`; `package-lock.json` criado; `.gitignore` confirma `node_modules/` ignored
- **Task 2:** Script implementado com `'use strict'`, CommonJS, ~210 linhas. Funções extraídas: `parseFrontmatter`, `countWords`, `getStatus`, `listMarkdownFiles`, `listSubdirs`, `validateAgents`, `validateDossiers`, `renderAgentsTable`, `renderDossiersTable`, `main`
- **Task 3:** Fixtures geradas via Node.js inline script com paragraph base de 50 palavras exatas (verificado). Word counts finais: agents-pass=705, agents-fail=405, research-pass=3800, research-fail=3000
- **Task 4 — Cenários:**
  - Cenário 1 (PASS): `node scripts/validate-density.js --root tests/fixtures/density/_scenarios/pass` → exit 0 (1 PASS + 1 WARN, esperado: 3800 está dentro de margin de 10% acima de 3500)
  - Cenário 2 (FAIL strict): exit 1 com 2 FAILs claros (405 + 3024 palavras)
  - Cenário 3 (FAIL --report-only): exit 0 mesmo com 2 FAILs
  - Cenário 4 (repo atual): exit 0 com "Nenhum agente/dossier encontrado"
- **Validations:**
  - Encoding: `file -I` confirma `charset=utf-8` em todos os 3 arquivos textuais
  - BOM: ausente em todos
  - CRLF: ausente em todos (LF only)
  - npm run validate:density funciona via npm script

### Completion Notes List

1. **Bonus feature implementada:** flag `--root <path>` para permitir validation contra paths arbitrários (útil para fixtures isoladas e CI custom). Não estava na story spec mas é low-risk e melhora testability.
2. **Fixtures geradas programaticamente** — paragraph base de 50 palavras exatas, multiplied por target / 50. Garante contagem determinística verificável.
3. **`research-pass` mostra WARN ao invés de PASS** — 3800 está entre 3500 e 3500×1.10=3850. Comportamento intencional (margin de 10% sinaliza dossiers borderline). Story AC dizia ≥3500; WARN não é FAIL, exit 0 mantido. Documentado em Architecture §5.3.
4. **Skip de `.gitkeep`** implementado via filter `entry.name !== '.gitkeep'`. Validado no cenário 4 (repo atual com 5 .gitkeep não foi processado como agent/dossier, output corretamente "Nenhum encontrado").
5. **Sem unit tests adicionais** — fixtures + manual validation cumprem o gate per story spec. Unit tests podem ser adicionados em v2.1 se necessário.
6. **YAML parse robusto** — função `parseFrontmatter` envolve `yaml.load` em try/catch e propaga erro via `main()` catch que retorna exit 2 (erro de sistema, distinto de FAIL=exit 1). Testado mentalmente; sem test case explícito de YAML mal-formado nos fixtures (out of scope).
7. **Tasks 1-5 todas OK em primeira tentativa**, sem retries. Sem deviations da spec exceto bonus `--root` flag.

### File List

**Created (10 files):**

- `scripts/validate-density.js` (~210 lines, ~7.5 KB) — script principal NFR1+NFR2 validation
- `tests/fixtures/density/agents-pass/sample-agent.md` (705 words body)
- `tests/fixtures/density/agents-fail/sample-agent.md` (405 words body)
- `tests/fixtures/density/research-pass/halbert/biografia.md` (~950 words)
- `tests/fixtures/density/research-pass/halbert/estilo.md` (~950 words)
- `tests/fixtures/density/research-pass/halbert/frameworks.md` (~950 words)
- `tests/fixtures/density/research-pass/halbert/exemplos.md` (~950 words)
- `tests/fixtures/density/research-fail/halbert/biografia.md` (~750 words)
- `tests/fixtures/density/research-fail/halbert/estilo.md` (~750 words)
- `tests/fixtures/density/research-fail/halbert/frameworks.md` (~750 words)
- `tests/fixtures/density/research-fail/halbert/exemplos.md` (~750 words)
- `package-lock.json` (auto-gerado por npm install — primeira dep externa do projeto)

**Modified (2 files):**

- `package.json` — version `2.0.0-draft.1 → 2.0.0-draft.2`; adicionado `dependencies.js-yaml: ^4.1.0` + `scripts.validate:density`
- `docs/stories/1.2.density-validation-script.md` — Dev Agent Record + tasks checkboxes + status

**Deleted:** None.

**Total:** 12 created + 2 modified = 14 files affected.

## QA Results

_To be populated by @qa após implementation_
