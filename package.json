{
  "name": "copy-squad",
  "version": "2.0.0-draft.4",
  "private": true,
  "description": "Copy Squad — 15 copywriters lendários como subagentes Claude Code, orquestrados via Copy Master",
  "repository": {
    "type": "git",
    "url": "https://github.com/valentecarlos/copy-squad"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "scripts": {
    "validate:density": "node scripts/validate-density.js",
    "validate:structure": "node scripts/validate-structure.js",
    "validate:research-format": "node scripts/validate-research-format.js",
    "validate": "npm run validate:density && npm run validate:structure && npm run validate:research-format"
  }
}
